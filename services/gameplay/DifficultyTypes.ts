import type { DifficultyOutputV2 } from '../difficulty/types';

/**
 * Result of difficulty calculation for gameplay consumers.
 * V1 compatibility aliases were removed.
 */
export type DifficultyOutput = DifficultyOutputV2;
