## 2024-07-14 - Optimize MovementSystem array iteration
**Learning:** Replaced `Array.prototype.forEach` with standard `for` loops in hot paths in `MovementSystem.ts` (`updateEnemies`, `updateSpeedLines`, `updateImpactRings`, `updateParticles`, `updateFloatingTexts`, and `updateDyingEnemies`). This avoids closure function allocations and garbage collection overhead during every tick of the movement system engine, resulting in improved performance (60fps target stability). Guard clauses (`if (entity === undefined) continue;`) are critical to prevent TypeErrors in sparse arrays representing pooled entities.
**Action:** Automatically replace `Array.prototype.forEach` loops with standard `for` loops in 60fps high-frequency rendering and physics systems, taking care to respect guard clauses for undefined pool entities and converting `return` to `continue` statements in loop bodies.
