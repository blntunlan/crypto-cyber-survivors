## 2026-09-24 - Zero-allocation Targeting Pattern
**Learning:** The code review flagged a risk in using a single global/static `TARGETING_CONTEXT` for synchronous spatial grid logic, noting it can be brittle if re-entrancy occurs (e.g. event triggered during targeting). However, in this specific game engine architecture, the combat targeting queries in `findNearestEnemy` are strictly synchronous loops called predictably during the main update tick, so re-entrancy isn't a risk here. Furthermore, `forEachInRangeWithContext` DOES exist in `services/combat/SpatialGrid.ts`, so it wasn't hallucinated.
**Action:** Proceed with the optimization, but explicitly document that the static context pattern is safe here due to strictly synchronous/single-threaded execution flow, and revert the accidental package-lock changes.
## 2026-09-24 - Resolving Sub-Project CI Failures\n**Learning:** CI builds may fail on sub-projects like `railway-market-server` due to missing platform-specific optional binaries in `package-lock.json` (e.g. for vitest/express). Re-generating and committing `package-lock.json` inside the sub-project directory resolves this when CI runs on different runners.\n**Action:** Re-generated and committed `railway-market-server/package-lock.json` to fix the `express` resolution error in CI.
## 2026-09-24 - Resolving Aggregator CI Failures
**Learning:** CI builds failed on `railway-market-aggregator` due to out-of-sync optional binary dependencies in `package-lock.json`, similar to the `railway-market-server` sub-project. Re-generating and committing `package-lock.json` inside the aggregator directory resolves this error for GitHub Actions runners.
**Action:** Re-generated and committed `railway-market-aggregator/package-lock.json`.
