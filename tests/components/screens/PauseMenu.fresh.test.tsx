import { describe, it, expect } from 'vitest';
import { PauseMenu } from '../../../components/screens/PauseMenu';

describe('PauseMenu freshness', () => {
  it('exports component', () => {
    expect(PauseMenu).toBeDefined();
  });
});
