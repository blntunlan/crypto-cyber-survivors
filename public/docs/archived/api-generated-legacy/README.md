# Archived Generated API Docs

This folder contains the older generated API documentation that used to live at `public/docs/api/`.

It was moved out of the active documentation surface on 2026-03-20 because the generated output was not being maintained in lockstep with the live Railway/runtime architecture.

If API docs are needed again, regenerate them from the current codebase and publish a fresh active surface instead of reviving these files as-is.
